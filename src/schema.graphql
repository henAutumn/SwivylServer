# import agency from "./generated/prisma.graphql"

type Query {
  feed: [Post!]!
  drafts: [Post!]!
  post(id: ID!): Post
  me: User
  agency: [Agency!]!
}

type Mutation {
  signup(email: String!, password: String!, name: String!): AuthPayload!
  login(email: String!, password: String!): AuthPayload!

  #Agenciesgit
  createAgency(id: ID!, masteruser: String!, name: String!, img: String!): Agency!
  # createDraft(title: String!, text: String!): Post!
  # publish(id: ID!): Post!
  # deletePost(id: ID!): Post!
  
  # kittens mutation
  # createKitten(name: String!, color: String!, fat: Boolean!, fierce: Boolean!): Kitten!
  # updateKitten(id: ID!, name: String, color: String, fat: Boolean, fierce: Boolean): Kitten!
  # deleteKitten(id: ID!): Kitten!
}

type Subscription {
  feedSubscription: PostSubscriptionPayload!
}

type AuthPayload {
  token: String!
  user: User!
}

type User {
  id: ID!
  email: String!
  password: String!
  name: String!
  posts: [Post!]!
  agency: Agency
}

type Agency {
  id: ID!
  masteruser: String!
  name: String!
  img: String!
  teammembers: [User!]!
}
