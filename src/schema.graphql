# import Agency from "./generated/prisma.graphql"

type Query {
  
  # agency(id: ID!): Agency!
  me: User
  # kittens:[Kitten!]!
}
# cjgs8x2iz3du40b06lib3xjag

type Mutation {
  signup(email: String!, password: String!, name: String!): AuthPayload!
  login(email: String!, password: String!): AuthPayload!
  createAgency(id:ID! masteruser:String! name:String! img:String!): Agency!
  # publish(id: ID!): Post!
  # deletePost(id: ID!): Post!
   
  # #kittens mutation

  # createKitten(name: String!, color: String!, isFat: Boolean!, isFierce: Boolean!): Kitten!
  # updateKitten(id:ID!, name: String, color: String, isFat: Boolean, isFierce: Boolean):Kitten!
  # deleteKitten(id:ID!):Kitten!
}

type Subscription {
  feedSubscription: PostSubscriptionPayload!
}

type AuthPayload {
  token: String!
  user: User!
  # agency: Agency!
}

type User {
  id: ID! 
  email: String! 
  password: String!
  name: String!
  posts: [Post!]!
  agency: Agency!
  # isMU: Boolean!
}
type Agency {
  id: ID! 
  masteruser: String!
  name: String!
  img: String!
  teammembers: [User!]
  }